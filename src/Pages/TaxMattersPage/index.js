import TaxMatters from "../../Components/TaxMatters";

const TaxMattersPage = () => {
  window.scrollTo({ left: 0, top: 0, behavior: "smooth" });
  return <TaxMatters />;
};
export default TaxMattersPage;
