import ServicesMatters from "../../Components/OtherLaws";

const ServicesMattersPage = () => {
  window.scrollTo({ left: 0, top: 0, behavior: "smooth" });
  return <ServicesMatters />;
};

export default ServicesMattersPage;
